import 'package:flutter/widgets.dart';

class CarrotFiller extends StatelessWidget {
  const CarrotFiller({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(),
    );
  }
}
